<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eosa.admin.mapper.CategoryMapper">

    <select id="selectCategory" resultType="CategoryDTO">
        SELECT
            C.categoryIdx,
            C.categoryName,
            C.categoryPrice
        FROM Category C
    </select>

    <insert id="priceUpdateCategory" parameterType="CategoryDTO">
        INSERT INTO Category(categoryIdx, categoryName, categoryPrice)
        VALUES(#{categoryIdx}, #{categoryName}, #{categoryPrice})
        ON DUPLICATE KEY UPDATE categoryPrice = #{categoryPrice}
    </insert>

    <delete id="deleteCategory" parameterType="Long">
        DELETE FROM Category
        WHERE categoryIdx = #{categoryIdx}
    </delete>

</mapper>