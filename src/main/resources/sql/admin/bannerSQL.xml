<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eosa.admin.mapper.BannerMapper">

    <insert id="bannerUpdate" parameterType="BannerDTO">
        INSERT INTO AdminBannerManage
            (bannerTag, bannerFileName, bannerFileLink)
        VALUES (#{bannerTag}, #{bannerFileName}, #{bannerFileLink})
    </insert>

    <delete id="bannerTruncate">
        TRUNCATE TABLE AdminBannerManage
    </delete>

<!--    <select id="countNoticeList" resultType="int">-->
<!--        SELECT COUNT(*)-->
<!--        FROM Notice N-->
<!--    </select> -->

    <select id="bannerList" resultType="BannerDTO">
        SELECT
        B.idx,
        B.bannerTag,
        B.bannerFileName,
        B.bannerFileLink
        FROM AdminBannerManage B
    </select>
<!--    -->
<!--    <update id="updateNoticeByNoticeIdx" parameterType="NoticeDTO">-->
<!--        UPDATE Notice N SET N.title = #{title}, N.content = #{content} WHERE N.idx = #{idx}-->
<!--    </update>-->

<!--    <delete id="deleteByNoticeIdx" parameterType="Long">-->
<!--        DELETE FROM Notice WHERE idx = #{idx}-->
<!--    </delete>-->

</mapper>