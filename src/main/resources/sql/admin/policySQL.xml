<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eosa.admin.mapper.PolicyMapper">

    <!-- 정책 조회 -->
    <select id="selectPolicy" resultType="PolicyDTO">
        SELECT P.policyIdx,
               P.policyTitle,
               P.policyContents,
               P.modifiedDate,
               P.policyCreateDate
        FROM Policy P
        ORDER BY P.policyIdx
    </select>

    <!-- 정책 수정 -->
    <update id="updatePolicy" parameterType="PolicyDTO">
        UPDATE Policy
        SET policyContents = #{policyContents},
            modifiedDate   = NOW()
        WHERE policyIdx = #{policyIdx}
    </update>

</mapper>
