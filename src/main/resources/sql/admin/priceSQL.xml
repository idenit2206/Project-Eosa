<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eosa.admin.mapper.PriceMapper">

    <select id="selectPrice" resultType="PriceDTO">
        SELECT
            P.priceIdx,
            P.bankName,
            P.bankNumber
        FROM Price P
        LIMIT 1
    </select>
    
    <insert id="updatePrice" parameterType="PriceDTO">
        INSERT INTO Price(priceIdx, bankName, bankNumber)
        VALUES(#{priceIdx}, #{bankName}, #{bankNumber})
        ON DUPLICATE KEY UPDATE priceIdx = #{priceIdx}
    </insert>


</mapper>