<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eosa.admin.mapper.RequestContractMapper">
    <select id="selectRequestContract" resultType="RequestContractDTO" parameterType="Long">
        SELECT
            requestContractIdx,
            requestFormIdx,
            requestContractContractId,
            companysIdx,
            usersIdx,
            requestContractCreateDate,
            requestContractTurn
        FROM RequestContract r
        WHERE r.requestFormIdx = #{requestFormIdx}
    </select>
</mapper>
